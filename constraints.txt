# Known-good toolchain lock for requirements-dev.txt / requirements-demo.txt / requirements-all.txt.
# pyserve itself is stdlib-only; use requirements.txt for a runtime-only editable install.
# Used by CI and for reproducible local setups:
#   pip install -r requirements-all.txt -c constraints.txt
pytest==9.0.3
pytest-cov==7.0.0
ruff==0.15.16
mypy==2.1.0
django==5.2.14
